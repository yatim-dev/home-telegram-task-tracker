services:
  bot:
    build: .
    image: task-tracker-bot:latest
    container_name: task-tracker-bot
    restart: unless-stopped

    env_file:
      - .env

    environment:
      DB_PATH: /data/tasks.db
      ADMIN_USER_ID: "449324524"

    volumes:
      - ./data:/data
